{
  "name": "ynab-helper",
  "version": "0.1.0",
  "main": "resources/main.js",
  "scripts": {
    "build-electron": "env LEIN_FAST_TRAMPOLINE=y lein trampoline cljsbuild auto electron-dev",
    "watch-electron": "sleep 10 && nodemon -w resources/main.js --exec electron .",
    "figwheel": "env LEIN_FAST_TRAMPOLINE=y rlwrap lein trampoline figwheel frontend-dev",
    "electron": "run-p -l build-electron watch-electron",
    "start": "run-p electron figwheel"
  },
  "devDependencies": {
    "electron": "^1.6.6",
    "nodemon": "^1.11.0",
    "npm-run-all": "^4.0.2"
  }
}
